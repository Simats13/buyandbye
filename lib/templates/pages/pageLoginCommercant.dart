import 'package:flutter/material.dart';
import 'package:buyandbye/templates/Connexion/Login/login_commercant_class.dart';

class PageLoginCommercant extends StatelessWidget {
  const PageLoginCommercant({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: LoginCommercant(),
    );
  }
}
