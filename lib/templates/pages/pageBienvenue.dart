import 'package:flutter/material.dart';
import 'package:oficihome/templates/Connexion/body.dart';

class PageBievenue extends StatelessWidget {
  const PageBievenue({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
